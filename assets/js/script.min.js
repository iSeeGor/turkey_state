"use strict";

var libs = 3; // Nice  Select

$(function () {
  preventDefaultLinks();
  selectCustom();
  filterTabsToggle();
  categoryLinksToggle();
  bgLinesResize();
  hamburgerToggle();
}); // Custom Select's

var selectCustom = function selectCustom() {
  $('select').niceSelect();
}; // Prevent Defult for all 'a' tag's


var preventDefaultLinks = function preventDefaultLinks() {
  var prventLinks = document.querySelectorAll('a');

  for (var i = 0; i < prventLinks.length; i++) {
    prventLinks[i].addEventListener('click', function (e) {
      e.preventDefault();
    });
  }
}; // Toggle for Filter Tabs


var filterTabsToggle = function filterTabsToggle() {
  var tabs = document.querySelectorAll('.filter-tab');

  function toggle(tab) {
    for (var i = 0; i < tab.length; i++) {
      tab[i].addEventListener('click', function (e) {
        var currentTab = this;

        for (var _i = 0; _i < tab.length; _i++) {
          if (currentTab == tab[_i]) {
            currentTab.classList.add('filter-tab--active');
          } else {
            tab[_i].classList.remove('filter-tab--active');
          }
        }

        e.preventDefault();
      });
    }
  }

  toggle(tabs);
}; // Toggle Class for Category List Links


var categoryLinksToggle = function categoryLinksToggle() {
  var catLinks = document.querySelectorAll('.category-list__link');

  for (var i = 0; i < catLinks.length; i++) {
    catLinks[i].addEventListener('click', function (e) {
      var currentLink = this;

      for (var _i2 = 0; _i2 < catLinks.length; _i2++) {
        if (currentLink != catLinks[_i2]) {
          catLinks[_i2].classList.remove('category-list__link--active');
        } else {
          currentLink.classList.add('category-list__link--active');
        }
      }

      e.preventDefault();
    });
  }
}; // Header Sticky + Add Class
// window.onscroll = () => { sticky() };
// let stickyHeader = document.querySelector('.site__header');
// let paddingTop = document.querySelector('.site');
// function sticky() {
//   if (document.body.scrollTop > 0 || document.documentElement.scrollTop > 0){
//     stickyHeader.classList.add('site__header--sticky');
//     paddingTop.style.paddingTop = '120px';
//   } else {
//     stickyHeader.classList.remove('site__header--sticky');
//     paddingTop.style.paddingTop = '0';
//   }
// };
// Background Lines Grig Risize


var bgLinesResize = function bgLinesResize() {
  (function () {
    var throttle = function throttle(type, name, obj) {
      obj = obj || window;
      var running = false;

      var func = function func() {
        if (running) {
          return;
        }

        running = true;
        requestAnimationFrame(function () {
          obj.dispatchEvent(new CustomEvent(name));
          running = false;
        });
      };

      obj.addEventListener(type, func);
    };

    throttle("resize", "optimizedResize");
  })();

  window.addEventListener("optimizedResize", function () {
    // call function background resize
    bgLinesResize();
  });

  var bgLinesResize = function bgLinesResize() {
    var firstBgLine = document.querySelector('.bg-lines>span');
    var container = document.querySelector('main .container');
    var containerWidth = container.clientWidth;
    var windowWidth = document.body.clientWidth;
    var bgLineMargin = 0;

    if (windowWidth <= 1920 && windowWidth >= 1500) {
      bgLineMargin = (windowWidth - containerWidth) / 2 - 192;
    } else {
      bgLineMargin = (windowWidth - containerWidth) / 2 - 1;
    } // bgLineMargin = (windowWidth - containerWidth)/2;


    firstBgLine.style.marginLeft = Math.floor(bgLineMargin) + 'px';
  };

  bgLinesResize();
}; // Hanburger Toggle


var hamburgerToggle = function hamburgerToggle() {
  var hamburger = document.querySelector('.mobile-header__hamburger');
  hamburger.addEventListener('click', function () {
    hamburger.classList.toggle('mobile-header__hamburger--active');
  });
};